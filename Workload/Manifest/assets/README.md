# Place workload assets (icons, images) here. The favicon.png from
# FabInspector.Web/wwwroot/favicon.png should be copied/symlinked into
# this folder by scripts/Build/BuildFrontend.ps1 (or committed directly).
